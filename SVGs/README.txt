Toilet sign SVG options converted from the generated PNG concepts.

Notes:
- These are automatic vector traces, not hand-redrawn CAD files.
- They are editable SVGs and should open in Inkscape, Illustrator, LightBurn, etc.
- For laser cutting, inspect/simplify nodes and remove any tiny unwanted specks before cutting.
- Each SVG is black filled paths on a transparent SVG canvas, with the original white background removed.
